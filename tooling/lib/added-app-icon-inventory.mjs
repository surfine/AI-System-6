// New applications extend the historical 56-object core without rewriting its
// accepted source batches. Consumers that display all artwork use both sets.
export const ADDED_APP_ICON_IDS = Object.freeze(["clioPaint", "clioProject", "oneMoreTune"]);
export const ADDED_APP_ICON_ERAS = Object.freeze(["classic", "platinum", "aqua", "snow-leopard", "yosemite", "big-sur", "liquid-glass", "nextstep"]);
