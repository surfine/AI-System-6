{
  "unwired": {
    "tooling/verify-bonsai-interop.mjs": "Maintainer-only real-browser Bonsai/Micropolis interop audit; it drives two games and Playwright on a merged build and is run by hand, not by a release or CI runner.",
    "tooling/verify-bonsai-remaster-assets.mjs": "Maintainer-only Bonsai remaster asset audit; it inspects generated art inputs and is run on demand, outside the release runner.",
    "tooling/verify-control-census.mjs": "Maintainer control-panel census ratchet run on demand via npm run verify:control-census; it needs a full browser sweep and is deliberately outside the release gate set.",
    "tooling/verify-doc-claims.mjs": "Maintainer-only claim audit of the agent knowledge layer; it is a report, deliberately not a gate, run by hand after editing the documents it checks.",
    "tooling/verify-demo-disks.mjs": "The thirty-four-disk sweep (npm run verify:demo-disks): it mounts every shipped demonstration disk through its own public /go link in a real browser and asserts the project, its six sections, six drafts, dossiers, manuscript and article window arrive whole. It runs after any change to a disk or to the import path; the release's batch lane already walks the DTK disk end to end (eight-stop-walk), so this is the wider sweep rather than a second copy of that gate.",
    "tooling/verify-live-release.mjs": "Post-publish acceptance of the deployed release (npm run verify:live-release): it needs the network and a live origin, so it cannot gate a build. It drives the deployed product in a browser — a shared disk link mounts and opens its article, the Control Panel applies the appearance the release added, and One More Tune's Relay starts a round without the answer in the client — and is the step a release runs after publishing, named in internal/operations/RELEASE.md.",
    "tooling/verify-hig.mjs": "Maintainer Human Interface Guidelines sweep run on demand via npm run verify:hig; it is a design audit surface, not a condition of the release pipeline.",
    "tooling/verify-ios-standalone.mjs": "Maintainer acceptance of the iPhone home-screen web app, run on demand via npm run verify:ios-standalone; it needs Xcode, a booted iPhone simulator and the baguette CLI, and it answers a question no browser instrument in this repository can ask, so it stays outside the release runner.",
    "tooling/verify-liquid-render.mjs": "Maintainer-only visual comparison for Liquid Glass rendering; it captures local frames by hand and is not wired into any shipping runner.",
    "tooling/verify-mcp-guest.mjs": "Maintainer browser acceptance for the MCP guest bridge; it spins a guest server and Playwright and is run by the MCP lane on demand, not by release gates.",
    "tooling/verify-micropolis-acceptance.mjs": "Maintainer-only Micropolis acceptance gate (npm run verify:micropolis-acceptance); it needs a local server and Playwright and stays out of the public source contract.",
    "tooling/verify-native-action-audit.mjs": "Audit for the frozen native Swift rewrite, run on demand via npm run verify:native-action-audit; the native port is not built or gated by this release.",
    "tooling/verify-native-parity-ledger.mjs": "Ledger audit for the frozen native Swift rewrite, run on demand via npm run verify:native-parity-ledger; kept as history while the native port stays frozen.",
    "tooling/verify-nextstep-edgecases.mjs": "Maintainer browser acceptance for the experimental NeXTSTEP shell (npm run verify:nextstep:edgecases); it drives the shelf, column navigation and dock edge cases against a running development server on --url, and its results are recorded per scenario in internal/evidence/drafts/nextstep/edgecases. The appearance is gated out of releases (releaseReady:false) and NS-11/21 are still open, so it is an acceptance instrument, not a release condition.",
    "tooling/verify-nextstep-workflow.mjs": "Maintainer browser acceptance for the experimental NeXTSTEP shell (npm run verify:nextstep:workflow); it walks the window, menu and resource scenes against a running development server on --url and writes internal/evidence/drafts/nextstep/workflow. Kept alongside verify-nextstep-edgecases.mjs as the pair the NeXTSTEP acceptance ledger cites, outside the release runner for the same reason.",
    "tooling/verify-one-more-tune-bank.mjs": "Maintainer bank check for One More Tune, run by hand as the design asks (\"提交前运行数据校验\"); the same function it calls, validateBank, is asserted by the one-more-tune feature contract, so a release gate does not need to run the command a second time.",
    "tooling/verify-theme-lab-fidelity.mjs": "Maintainer theme-lab fidelity capture (npm run verify:theme-lab:fidelity); it compares local screenshot boards by hand and is not part of verify:release.",
    "tooling/verify-web-vitals.mjs": "Performance budget probe for the phone and laptop start-up path, run on demand; a wall clock on a shared machine is not repeatable enough to refuse a release, so it reports numbers rather than gating them.",
    "tooling/visual-snapshot.mjs": "Superseded local visual-snapshot utility kept for maintainer inspection; the appearance snapshot and token tables own pixel coverage now."
  }
}
